using Newtonsoft.Json.Linq;

namespace WebApiBia.Dashboard.UiControls.Chart.Series.Marker
{
    public class LineSeriesPowerDemandDetailMarkerJobject
    {
       public JObject Generate()
        {
            JObject marker = new JObject();
            marker["radius"] = 1;
            marker["stroke"] = "#FFFFFF";
            marker["color"] = "#CACA9E";

            return marker;

           /*
            * marker: {
                opacity: 0,
                scaling: 0.01,
                fx: {
                    duration: 200,
                    easing: 'easeOut'
                     }
                 }
            * 
            */

        }
    }
}
